# Link checker config. Offline = verify only relative/internal links between
# repo files (deterministic in CI); external URLs are not network-fetched here.
offline = true
include_fragments = false
exclude_path = ["node_modules", ".git"]
